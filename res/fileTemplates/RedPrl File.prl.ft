/*
 * Created by Dependently-Typed Lambda Calculus on ${YEAR}-${MONTH}-${DAY}
 * ${NAME}
 * Author: ${USER}
 */

theorem ${NAME_CAMEL}:
  (-> bool bool) // Goal
by {
  // Tactic expression
  lam b =>
    if b then `ff else `tt
}.
